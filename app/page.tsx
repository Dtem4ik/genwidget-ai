export default function Home() {
  return (
    <div className="flex flex-1 flex-col items-center justify-center gap-3 px-4 text-center">
      <h1 className="text-3xl font-semibold tracking-tight">Widgetloom</h1>
      <p className="text-muted-foreground max-w-md text-balance">
        AI chat that answers with live React widgets instead of text. Under construction — chat core
        lands in phase 1.
      </p>
    </div>
  );
}
